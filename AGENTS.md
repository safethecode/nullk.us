# nullk.us 작업 가이드

이 문서는 저장소 전체에 적용된다. 더 하위의 `AGENTS.md`가 있다면 해당 디렉터리에서는 하위 문서를 우선한다.

## 프로젝트 맥락

- `nullk.us`는 프론트엔드 엔지니어의 개인 포트폴리오이다. 단순한 정보 나열보다 방문자가 느끼는 인상, 세밀한 타이포그래피, 부드러운 인터랙션이 중요하다.
- `pnpm` + Turborepo 모노레포이며, 메인 Next.js App Router 앱은 `apps/service`에 있다.
- 실제 버전과 스크립트는 README의 설명보다 `package.json`과 잠금 파일을 우선해 판단한다.
- 홈은 인물 사진 콜라주, 편집적 키워드, 중앙 정렬된 삼손 로고와 텍스트 메뉴, 오른쪽 하단 Question UI가 핵심이다. 홈에서는 공용 푸터를 노출하지 않는다.
- `/incubator`는 진행한 프로젝트를 로고, 제목, URL로 보여주는 전체 화면 그리드다. 카드는 화면에서 잘리지 않아야 하고, 호버 툴팁은 등장과 퇴장 모두 스프링처럼 부드러워야 한다.
- 게시글은 현재 메인 경험의 중심이 아니다. 명시적인 요청 없이 게시글을 작성, 수정, 등록, 제출, 공개하지 않는다.

## 커뮤니케이션과 진행 방식

- 한국어로 짧고 명확하게 보고한다. 수행 과정보다 결과를 먼저 말한다.
- 작은 UI 조정, 문구 변경, 간격·크기·정렬 수정, 명확한 버그 수정은 되묻지 말고 바로 진행한다.
- 결과가 크게 달라지는 제품 방향, 아키텍처, 외부 공개 범위, 데이터 삭제, 비용 발생, 되돌리기 어려운 작업에만 확인을 구한다.
- 사용자가 `그냥 보려고`, `테스트 코드 짜지 마`, `물어보지 마` 같이 진행 방식을 지정하면 그 지시를 우선한다.
- 동일한 확인을 반복하거나 검증 과정을 길게 설명하지 않는다.

## UI와 레이아웃 원칙

- 모바일과 데스크톱을 항상 같이 고려한다. 모바일에서 타이틀이 지나치게 작아지거나, 이미지가 화면을 밀어내거나, 카드가 잘리지 않도록 한다.
- 여백의 책임을 축별과 컨테이너별로 나눈다. 예를 들어 전체 컨테이너가 `px-*`로 수평 여백을 담당하고, 헤더 내부가 `py-*`로 수직 여백을 담당하는 구조를 선호한다.
- 수평과 수직 여백의 의미가 다르면 `p-*`로 퉁치지 말고 `px-*`, `py-*`로 나눈다. 정말 네 방향의 값과 책임이 같을 때만 `p-*`를 쓴다.
- 마크업은 탑다운으로 읽힌다. 뒤 요소의 `mt-*`로 앞 요소와의 관계를 만들기보다 부모의 `gap-*`, 앞 요소의 하단 여백, 컨테이너 높이 같은 구조적 방식을 우선한다.
- flex/grid 자식 사이 관계는 개별 마진보다 `gap-*`으로 표현한다. 뒤에 요소가 없는 마지막 자식에 의미 없는 하단 마진을 주지 않는다.
- `패딩 없이 꽉 채우기`가 요구된 영역에는 암묵적인 패딩을 넣지 않는다. 필요한 여백은 상위 레이아웃이 아니라 여백이 필요한 개별 요소에서 명시한다.
- 타이포그래피는 디자인 맥락에 맞는 실제 폰트를 선택한다. 특정 폰트와 비슷한 느낌을 요청받았다고 무조건 JetBrains Mono로 대체하지 않는다.
- 자간을 지나치게 넓히거나 정형적으로 만들어 `AI가 만든 듯한` 인상을 주지 않는다. 영문 문구는 짧고 자연스럽게 쓴다.
- 호버·툴팁·플로팅 UI는 공간과 움직임이 자연스럽게 연결되어야 한다. 툴팁은 등장뿐 아니라 사라질 때도 부드러운 움직임을 적용한다.
- 스크롤 안내 같은 작은 요소도 실제로 움직이는지 확인한다. `transform` 애니메이션 대상이 비대체 인라인 요소라면 `inline-block` 등 변환 가능한 display를 사용한다.

## 자산과 이미지

- 외부 URL의 이미지, SVG, 폰트를 제품에서 직접 핫링크하지 않는다. 라이선스와 출처를 확인한 뒤 `apps/service/public/assets` 또는 `apps/service/styles/fonts`에 내려받아 사용한다.
- 레이아웃에 필요한 이미지는 비율을 유지하고 컨테이너 안에서 잘리지 않게 만든다. 특히 고정 크기 그리드는 현재 뷰포트에 들어가는 행과 열을 계산해 완전한 카드만 노출한다.
- 사진 보호 요구가 있는 영역은 선택, 드래그, 롱프레스 같은 일반적인 저장 동작을 억제하고, 페이지 메타데이터와 자산 응답 헤더, `robots.txt`를 함께 고려한다. 다만 브라우저에 전송된 이미지의 다운로드를 완전히 차단할 수 있다고 설명하지 않는다.

## 코드 구조

- App Router의 Server Component를 기본으로 유지하고 상태, 이벤트, 브라우저 API가 필요한 최소 영역만 Client Component로 분리한다.
- 페이지는 `apps/service/app`, 공용 UI는 `apps/service/ui`, 도메인 로직과 상수는 `apps/service/lib`, 전역 스타일은 `apps/service/styles`의 기존 구조를 따른다.
- 이미지는 기본적으로 `next/image`를 사용한다. 검색 제외용 응답 헤더처럼 원본 URL이 필요한 명확한 이유가 있을 때만 `unoptimized`를 선택한다.
- 지금 작업과 관계없는 리팩터링, 패키지 교체, 콘텐츠 수정을 끼워 넣지 않는다.

## 검증

- 검증은 변경 위험에 비례해 최소한으로 수행한다. 단순 UI 조정에 무거운 전체 빌드나 관계없는 검증을 반복하지 않는다.
- 한 페이지의 표현 변경은 해당 테스트와 `pnpm --filter service check-types` 정도를 기본으로 한다.
- 라우팅, 메타데이터, 서버 설정, 공용 로직을 바꾼 경우 `pnpm --filter service test`를 실행한다.
- 시각 작업은 가능하면 브라우저에서 뷰포트, 잘림, 호버, 애니메이션을 확인한다. 브라우저 확인이 불가능하면 그 사실만 짧게 알린다.
- 사용자가 미리보기만 원하며 테스트 작성을 제외했다면 새 테스트를 추가하지 않는다.

주요 명령:

```bash
pnpm --filter service dev
pnpm --filter service test
pnpm --filter service check-types
pnpm lint
```

## Git과 공개 원칙

- 작업 시작 전 현재 브랜치, 원격, 사용자의 기존 변경을 확인한다. 관계없는 수정이 있어도 그 상태를 보존하고 작업 범위만 명시적으로 스테이징한다.
- 커밋은 관심사별로 세세하게 나눈다. UI, 동작, SEO, 테스트, 문서가 독립적이면 가능한 별도 커밋으로 남긴다.
- 메시지는 기존 `git log`를 참고해 Karma/Conventional 형식의 `type(scope): 한국어 요약`으로 작성한다.
- scope로 맥락을 드러낸다. 요약에 `인큐베이터` 같은 페이지 이름을 반복하기보다 `feat(incubator): 프로젝트 툴팁 추가`처럼 쓴다.
- 일반적인 type은 `feat`, `fix`, `style`, `refactor`, `test`, `docs`, `chore`를 사용한다.
- 사용자가 커밋이나 푸시를 금지하지 않았다면, 요청한 작업을 완료한 뒤 적절히 나눈 커밋을 만들고 현재 업스트림 브랜치에 푸시한다.
- 강제 푸시, 히스토리 재작성, 삭제, 배포, PR 생성, 외부 서비스에 데이터를 보내는 작업은 명시적인 지시 없이 수행하지 않는다.
- 게시글, 작성 중인 문서, 미추적 새 파일은 현재 요청의 범위에 포함된다는 근거가 없으면 커밋하지 않는다.
